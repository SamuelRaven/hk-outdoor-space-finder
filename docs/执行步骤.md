# 执行步骤

> 按顺序分阶段执行，不要一口气做完。每完成一个 Phase 停下来确认。

---

## Phase 1：基础框架（无可见效果）

| # | 文件 | 说明 |
|---|------|------|
| 1.1 | `index.html` | HTML 骨架：viewport meta、Google Fonts 引用、4个页面容器 `<section>` |
| 1.2 | `css/bauhaus-tokens.css` | CSS 自定义属性（颜色、字体、间距） |
| 1.3 | `css/reset.css` | Box-sizing、清除默认样式 |
| 1.4 | `css/layout.css` | 移动优先布局、页面显隐切换 |
| 1.5 | `js/core/router.js` | Hash 路由：register、navigate、hashchange 监听 |

## Phase 2：首页（第一个可见页面）

| # | 文件 | 说明 |
|---|------|------|
| 2.1 | `css/components.css` | 按钮基础样式（.btn、.btn-primary、.btn-secondary） |
| 2.2 | `index.html` | 首页结构：标题 + 两个按钮 + 几何装饰 |
| 2.3 | `js/ui/landing-page.js` | 按钮1跳转筛选页、按钮2弹出 toast |
| 2.4 | `js/ui/toast.js` | Toast 组件（底部居中，2秒消失） |

## Phase 3：筛选页（核心交互）

| # | 文件 | 说明 |
|---|------|------|
| 3.1 | `js/data/districts.json` | 大区→小区映射数据 |
| 3.2 | `index.html` | 筛选页结构：4个选择器 + GO 方形按钮 + 盲盒按钮 |
| 3.3 | `css/components.css` | 筛选器、chips、GO 按钮、盲盒按钮样式 |
| 3.4 | `js/ui/filter-page.js` | 状态机、级联逻辑、GO 验证、盲盒跳转 |

## Phase 4：结果页

| # | 文件 | 说明 |
|---|------|------|
| 4.1 | `js/data/parks.json` | 先整理 15 个核心公园数据 |
| 4.2 | `js/core/matcher.js` | 硬过滤 + 加分排序算法 |
| 4.3 | `index.html` | 结果页结构：标题 + 可滚动卡片列表 |
| 4.4 | `css/components.css` | 卡片样式（1px 边框 + 左侧色条） |
| 4.5 | `js/ui/results-page.js` | 读取筛选 → 匹配 → 渲染卡片 |

## Phase 5：盲盒页

| # | 文件 | 说明 |
|---|------|------|
| 5.1 | `index.html` | 盲盒页结构：文字 + 动画容器 + 卡片 |
| 5.2 | `js/ui/dice-animation.js` | CSS 骰子动画（可选） |
| 5.3 | `js/ui/blindbox-page.js` | 随机选公园 + 展示 |

## Phase 6：打磨收尾

| # | 文件 | 说明 |
|---|------|------|
| 6.1 | `js/app.js` | 入口：注册路由、启动 |
| 6.2 | `js/data/parks.json` | 补全全部 ~46 个公园 |
| 6.3 | 全局 | 响应式微调、真机测试 |

---

## 规则

1. **按 Phase 顺序执行**，完成一个 Phase 后暂停，等确认再继续
2. 每个 Phase 内的步骤也**逐个文件创建**，不要批量产出
3. 遇到数据不确定的地方（如某个公园的 openingHours），标注 `TODO`，不要编造
